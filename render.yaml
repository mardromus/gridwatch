services:
  - type: web
    name: gridwatch
    runtime: docker
    plan: free
    healthCheckPath: /api/health
    envVars:
      - key: OPENAI_MODEL
        value: gpt-4.1-mini
      - key: CORS_ORIGINS
        value: "*"
      - key: GRIDWATCH_DB_PATH
        value: /tmp/gridwatch.db